server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server # default-label = branch
          uri: https://github.com/Distopya/Microservice #link of the repository
          search-paths: config-data #search-paths search the archive
  #          username: ${GIT_USER}
  #          password: ${GIT_PASSWORD}
  #  security:
  #    user:
  #      name: Hugo
  #      password: Rushia
  application:
    name: config-server